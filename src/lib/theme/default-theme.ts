/** Institutional values live here, never in Blocks or editorial field helpers. */
export const DEFAULT_PALETTE = Object.freeze({
  background: "#ffffff",
  foreground: "#1e293b",
  brand: "#0a3299",
  action: "#0a3299",
  accent: "#5cd6c9",
});

export const NEUTRAL_COLORS = Object.freeze({
  light: "#ffffff",
  dark: "#000000",
  surface: "#ffffff",
  surfaceForeground: "#1e293b",
  muted: "#f1f5f9",
  mutedForeground: "#475569",
  border: "#cbd5e1",
});

/** Not configurable through branding. */
export const SYSTEM_COLORS = Object.freeze({
  focus: "#14b1f2",
  success: "#167c4a",
  warning: "#b86e00",
  danger: "#b42318",
});
